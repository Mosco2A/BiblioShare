import 'package:flutter/material.dart';

/// Palette de couleurs BiblioShare
class AppColors {
  AppColors._();

  // Primary — Bleu profond (confiance, culture)
  static const Color primary = Color(0xFF1B3A5C);
  static const Color primaryLight = Color(0xFF2E5C8A);
  static const Color primaryDark = Color(0xFF0F2440);

  // Secondary — Ambre chaud (énergie, convivialité)
  static const Color secondary = Color(0xFFE8A838);
  static const Color secondaryLight = Color(0xFFF0C060);
  static const Color secondaryDark = Color(0xFFD09020);

  // Accent — Corail (social, recommandations)
  static const Color accent = Color(0xFFE06050);

  // Background
  static const Color background = Color(0xFFF8F6F2);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF0EDE8);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textHint = Color(0xFF9E9E9E);

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFE53935);
  static const Color info = Color(0xFF2196F3);

  // Dark mode
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkSurfaceVariant = Color(0xFF2C2C2C);
}
